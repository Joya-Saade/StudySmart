{
  "name": "wasp-dev",
  "build": { "dockerfile": "Dockerfile" },
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "18" }
  },
  "postCreateCommand": "bash -lc 'curl -sSL https://get.wasp.sh/installer.sh | sh && echo export PATH=\"$HOME/.wasp/bin:$PATH\" >> ~/.bashrc && wasp version || true'"
}
